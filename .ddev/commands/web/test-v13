#!/bin/bash

## Description: Run unit + functional tests against TYPO3 13.4 LTS (SQLite, mirrors CI)
## Usage: test-v13
## Example: ddev test-v13

set -euo pipefail

EXTENSION_DIR=/var/www/content_api
cd "$EXTENSION_DIR"

PHP_VERSION=$(php -r 'echo PHP_MAJOR_VERSION . "." . PHP_MINOR_VERSION;')

echo "==> Saving current composer state..."
cp composer.json /tmp/composer.json.test-bak
cp composer.lock /tmp/composer.lock.test-bak

cleanup() {
    echo ""
    echo "==> Restoring original composer state..."
    cp /tmp/composer.json.test-bak composer.json
    cp /tmp/composer.lock.test-bak composer.lock
    composer install --no-progress --prefer-dist -q
    echo "==> Restore complete."
}
trap cleanup EXIT

echo "==> Switching to TYPO3 ^13.4..."
composer require typo3/cms-core:"^13.4" --no-progress --prefer-dist

echo ""
echo "================================================================"
echo " Unit Tests  |  PHP ${PHP_VERSION}  |  TYPO3 13.4"
echo "================================================================"
composer run-script test:unit

echo ""
echo "================================================================"
echo " Functional Tests  |  PHP ${PHP_VERSION}  |  TYPO3 13.4  |  SQLite"
echo "================================================================"
typo3DatabaseDriver=pdo_sqlite composer run-script test:functional

echo ""
echo "================================================================"
echo " ALL TESTS PASSED  |  PHP ${PHP_VERSION}  |  TYPO3 13.4"
echo "================================================================"
